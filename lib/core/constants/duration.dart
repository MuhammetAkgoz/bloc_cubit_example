abstract class DurationConst {
  static const Duration DURATION_VERY_VERY_LOW = Duration(milliseconds: 10);

  /// 150ms
  static const Duration DURATION_VERY_LOW = Duration(milliseconds: 150);

  /// 500ms
  static const Duration DURATION_LOW = Duration(milliseconds: 500);

  /// 1s
  static const Duration DURATION_NORMAL = Duration(seconds: 1);

  /// 2s
  static const Duration DURATION_HIGH = Duration(seconds: 2);

  /// 10s
  static const Duration DURATION_TEN_SECOND = Duration(seconds: 10);
}
